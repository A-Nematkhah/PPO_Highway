torch>=2.0
gymnasium>=0.29
highway-env>=1.8
numpy>=1.24
matplotlib>=3.7

# optional: also used by EUREKA (python -m eureka.loop) via key_manager;
# not only for the orphan USE_LLM_JUDGE path in config.py
groq>=0.9

# dev / CI
pytest>=8.0
pytest-cov>=5.0
